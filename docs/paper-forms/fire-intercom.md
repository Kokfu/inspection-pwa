# Fire Intercom - paper form transcription

Source: `SERVICE REPORT SAMPLE.pdf` p.10 (blank master).
Not present as a filled section in MAK SITI or the Hokuden Zone 1/2/3 reports.

Page title: **FIRE INTERCOM SYSTEM**

Revision differences: none observed (no Revision-B example exists for this section).

## Header fields

None. There is no `Date tested`, no panel/location field — just the page title and the
table.

## Blocks

### Station schedule — repeatable_table

A single grid. Column-group headers, left to right:

- (row label column — no header)
- **`Condition`** / **`Yes`** — with two sub-columns headed **`1`** and **`2`**
- **`Condition`** / **`No`** — with two sub-columns headed **`1`** and **`2`**
- `Comments :` — free-text ruled lines to the right, spanning the grid height

Row label column — pre-printed values, top to bottom as printed on the page:
`9`, `8`, `7`, `6`, `5`, `4`, `3`, `2`, `1`, `Grd Floor`, `Basement`, `Genset`,
`Pump Room`, and one final blank row.

| column key | printed header | control | allowed values | notes |
|---|---|---|---|---|
| station | *(no header)* | pre-printed label / write-in for the blank last row | `9`…`1`, `Grd Floor`, `Basement`, `Genset`, `Pump Room`, blank | printed high-to-low |
| condition_yes_1 | Condition / Yes → 1 | box | hand mark | meaning of "1" not printed |
| condition_yes_2 | Condition / Yes → 2 | box | hand mark | meaning of "2" not printed |
| condition_no_1 | Condition / No → 1 | box | hand mark | meaning of "1" not printed |
| condition_no_2 | Condition / No → 2 | box | hand mark | meaning of "2" not printed |
| comments | Comments : | ruled lines (shared, to the right of the whole grid) | free text | one comments area for the section, not per row |

## Result legend

**None printed on this page.** Page 10 has no `Remarks :- ( / ) ... ( X ) ...` footer, and
no other legend.

## Ambiguities

- **What "1" and "2" mean under `Condition Yes` and under `Condition No` is not stated
  anywhere on the page.** Not inferred. (Per the task brief: record as an ambiguity, do
  not infer.)
- No result legend on the page at all — how the boxes are meant to be marked (tick, `1`/`2`
  count, cross) is not defined.
- The row labels count down `9 … 1` then list named plant rooms; whether `1`–`9` are floor
  numbers is not printed.
- Only one `Comments` area is printed for the whole section; it is not clear if it is
  per-row or per-section (its height spans the whole grid, suggesting per-section).
- No field marked mandatory. No Revision-B example to compare against.
