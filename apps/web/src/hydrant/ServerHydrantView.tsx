import type { ServerHydrantDetail } from "./serverHydrantApi";
const labels:[string,string][]=[["Canvas Hose 1","canvasHose1Result"],["Canvas Hose 2","canvasHose2Result"],["Diffuser Nozzle","diffuserNozzleResult"],["Landing Valve","landingValveResult"],["Landing Valve Handle","landingValveHandleResult"],["Hose Cabinet","hoseCabinetResult"],["Key Lock","keyLockResult"]];const label=(x:unknown)=>x==="good"?"Good":"Poor";
export function ServerHydrantView({inspection,onBack}:{inspection:ServerHydrantDetail;onBack:()=>void}){return <section className="hose-reel-form server-inspection-detail" aria-labelledby="accepted-hydrant-title"><button type="button" className="secondary-command" onClick={onBack}>Back to Systems</button><header className="inspection-context"><div><p className="eyebrow">{inspection.jobReference}</p><h2 id="accepted-hydrant-title">{inspection.systemLabel}</h2><p>{inspection.jobTitle}</p><p>{inspection.customerName}</p></div><strong className="inspection-status status-synced">Inspection Complete</strong></header><p className="success-message">Submitted {new Date(inspection.performedAt).toLocaleString()} · Synced by {inspection.syncedByUsername}</p><section><h3>Hydrant Type</h3><p>{inspection.responses.hydrantType}</p></section><section><h3>Hydrant Location Rows</h3>{inspection.responses.rows.map((row,index)=><section className="hose-check-row" key={row.rowUuid}><strong>#{index+1}</strong><p>No. {row.assetReference||"Not recorded"}</p><p>Location: {row.locationText}</p>{labels.map(([name,key])=><p key={key}>{name}: {label(row[key as keyof typeof row])}</p>)}<p>{row.remarks||"No remarks"}</p></section>)}</section><section><h3>Comments</h3><p>{inspection.responses.comments||"No comments"}</p></section></section>}
