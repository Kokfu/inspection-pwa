# PWA Notes

The service worker is generated by `vite-plugin-pwa` during production builds.

Cache Storage is for app-shell assets only. Business data, drafts, sync outbox items, offline reference data, and future attachment Blobs belong in IndexedDB.

